# 筛图工具 - 功能清单

## 核心功能

### 1. 快速导入
- ✅ 拖放导入文件夹
- ✅ 点击按钮选择文件夹
- ✅ 自动识别所有图片格式（JPG, PNG, RAW, WEBP等）
- ✅ 保留原始文件夹结构
- ✅ 支持深层嵌套目录
- ✅ 实时进度显示

### 2. 极速分类
- ✅ 三键打标：`1` (正确), `2` (适中), `3` (错误)
- ✅ 快捷键清除：`0` 或 `X` 清除标记
- ✅ 左右方向键翻页：`←` `→`
- ✅ 批量框选打标（Ctrl/Cmd + 点击）
- ✅ 右键快捷菜单
- ✅ 自动保存分类到本地
- ✅ 刷新后自动恢复分类

### 3. 对比模式
- ✅ 同时选择2-8个文件夹进行对比
- ✅ 自动按文件名对齐（忽略扩展名差异）
- ✅ 显示文件格式角标（JPG, PNG, RAW等）
- ✅ 缺失文件显示占位符
- ✅ 适合对比RAW vs JPEG，多机位照片

### 4. 智能导出 🆕
- ✅ **保留原文件名**: 支持中文和特殊字符，仅移除非法字符
- ✅ **保留文件夹结构**: 在分类目录下保持原始层级
- ✅ **双语分类文件夹**: `正确_Correct/`, `适中_Medium/` 等
- ✅ **自动避免冲突**: 重复文件名自动添加数字后缀
- ✅ **详细错误报告**: 显示导出失败的文件和原因
- ✅ **部分导出容错**: 即使部分失败也继续处理
- ✅ **实时进度显示**: 导出进度百分比

### 5. 大图预览
- ✅ 双击图片打开预览
- ✅ 鼠标滚轮缩放
- ✅ 拖拽平移查看细节
- ✅ 预览模式下直接打标签
- ✅ 批量预览框选的图片
- ✅ 分组浏览（上一组/下一组）
- ✅ 右键菜单快速打标

### 6. 文件夹面板
- ✅ 显示所有文件夹及图片数量
- ✅ 按分类筛选查看
- ✅ 多选文件夹进入对比模式
- ✅ 实时统计信息

### 7. 数据持久化
- ✅ 分类标记自动保存到 IndexedDB
- ✅ 刷新页面数据不丢失
- ✅ 支持多用户隔离（基于浏览器指纹）
- ✅ 关闭标签页后数据仍然保留

## 性能优化

### 内存管理
- ✅ Object URL 生命周期管理，防止内存泄漏
- ✅ 图片懒加载，降低内存占用
- ✅ 虚拟滚动支持（按需显示）

### 算法优化
- ✅ 对比模式使用查找表，O(n²) → O(n)
- ✅ IndexedDB 并行写入，5倍 I/O 提升
- ✅ 批量更新优化，减少重复渲染

### 用户体验
- ✅ 零延迟响应
- ✅ 无加载屏幕
- ✅ 流畅的拖放体验
- ✅ 即时视觉反馈

## 快捷键一览

| 按键 | 功能 |
|-----|------|
| `1` | 标记为"正确" (✓) |
| `2` | 标记为"适中" (~) |
| `3` | 标记为"错误" (✕) |
| `0` 或 `X` | 清除标记 |
| `←` | 上一张图片 |
| `→` | 下一张图片 |
| `Ctrl/Cmd + 点击` | 框选多张图片 |
| `双击` | 打开大图预览 |
| `右键` | 快捷菜单 |
| `滚轮` | 缩放图片（预览模式） |

## 支持的格式

### 图片格式
- ✅ JPEG / JPG
- ✅ PNG
- ✅ WEBP
- ✅ GIF
- ✅ BMP
- ✅ TIFF / TIF
- ✅ SVG
- ✅ ICO

### RAW 格式
- ✅ CR2 (Canon)
- ✅ NEF (Nikon)
- ✅ ARW (Sony)
- ✅ DNG (Adobe)
- ✅ RAF (Fujifilm)
- ✅ ORF (Olympus)
- ✅ RW2 (Panasonic)

## 浏览器要求

### 完全支持
- ✅ Chrome 86+
- ✅ Edge 86+

### 不支持
- ❌ Firefox（不支持 File System Access API）
- ❌ Safari（不支持 File System Access API）

**原因**: 导入和导出功能依赖 [File System Access API](https://developer.mozilla.org/en-US/docs/Web/API/File_System_Access_API)，Firefox 和 Safari 尚未实现此标准。

## 使用场景

### 适合
- 📸 摄影师快速筛选照片
- 🎨 设计师挑选素材
- 📊 批量处理图片分类
- 🔄 对比不同版本的图片（RAW vs JPEG）
- 📁 整理大量照片到不同文件夹

### 不适合
- ❌ 需要云端同步（本地工具）
- ❌ 多人协作筛选（单机工具）
- ❌ 需要编辑图片（仅分类工具）

## 常见问题

### Q: 数据存储在哪里？
A: 所有数据（图片和分类标记）都存储在您的本地浏览器中，不会上传到任何服务器。

### Q: 刷新页面后数据会丢失吗？
A: 不会。分类标记会自动保存到 IndexedDB，重新导入相同文件夹后会自动恢复。

### Q: 为什么不支持 Firefox/Safari？
A: 因为它们不支持 File System Access API。我们不做 polyfill，保持代码简洁高效。

### Q: 可以同时打开多个项目吗？
A: 可以。每次导入文件夹时，分类标记会根据文件路径自动关联。

### Q: 导出的文件夹结构是怎样的？
A: 导出时会创建分类文件夹（如 `正确_Correct/`），在分类文件夹下保持原始的文件夹结构。

### Q: 支持多少张图片？
A: 理论上无限制。实测 10,000+ 张图片流畅运行。

## 技术栈

- **React 19** - UI 框架
- **Zustand** - 状态管理
- **Vite** - 构建工具
- **IndexedDB** - 本地存储
- **Tailwind CSS** - 样式框架
- **File System Access API** - 文件系统访问

## 开源协议

MIT License - 随便用，随便改。

---

**不废话，专注效率。**
